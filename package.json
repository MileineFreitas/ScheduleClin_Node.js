{
  "name": "scheduleclin",
  "version": "1.0.0",
  "private": true,
  "description": "ScheduleClin — sistema de agendamento para clínica de psicologia",
  "scripts": {
    "start": "npm start --prefix ScheduleClin_Node",
    "dev": "npm run dev --prefix ScheduleClin_Node",
    "postinstall": "npm install --prefix ScheduleClin_Node",
    "prisma:generate": "npm run prisma:generate --prefix ScheduleClin_Node",
    "prisma:studio": "npm run prisma:studio --prefix ScheduleClin_Node"
  },
  "engines": {
    "node": ">=18"
  }
}
